---
modules:
  default:
    metrics:
    - name: random_number_value
      path: '{ .random_number }'
      help: Example of a top-level global value scrape in the json
      labels:
        env: test
        timestamp: '{ .timestamp }'
        id: '{ .unique_id }'
